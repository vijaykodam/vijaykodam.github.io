<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Co-Authored with AI on Vijay Kodam</title><link>https://vijay.eu/co-authored/</link><description>Recent content in Co-Authored with AI on Vijay Kodam</description><generator>Hugo -- gohugo.io</generator><language>en</language><lastBuildDate>Sat, 14 Feb 2026 18:30:00 +0300</lastBuildDate><atom:link href="https://vijay.eu/co-authored/index.xml" rel="self" type="application/rss+xml"/><item><title>LLM Inference Internals: KV Cache, Flash Attention, and Optimizing for Apple Silicon</title><link>https://vijay.eu/co-authored/llm-inference-internals-apple-silicon/</link><pubDate>Sat, 14 Feb 2026 18:30:00 +0300</pubDate><guid>https://vijay.eu/co-authored/llm-inference-internals-apple-silicon/</guid><description>&lt;h2 id="introduction"&gt;Introduction&lt;/h2&gt;
&lt;p&gt;This is a companion post to my &lt;a href="https://vijay.eu/co-authored/ollama-claude-code-performance-guide/"&gt;Claude Code + Ollama troubleshooting guide&lt;/a&gt;, where I debugged why a local LLM session took 1 hour instead of 9 minutes. That post covers the practical diagnosis and fixes. This one gets into the inference internals that explain &lt;em&gt;why&lt;/em&gt; local LLM inference behaves the way it does.&lt;/p&gt;
&lt;p&gt;If you work with local LLMs (whether through Ollama, llama.cpp, MLX, or any other engine), understanding these internals will help you reason about performance, tune parameters effectively, and avoid pitfalls that waste memory and time.&lt;/p&gt;
&lt;p&gt;The reference model throughout this post is &lt;strong&gt;qwen3-coder-next:q4_K_M&lt;/strong&gt; (79.7B parameters, Q4_K_M quantization, 48 layers) running on a &lt;strong&gt;Mac Mini M4 Pro with 64GB unified memory&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;&lt;img src="https://vijay.eu/images/llm-inference-insider.webp" alt="llm-inference"&gt;&lt;/p&gt;
&lt;p&gt;Credits: Generated using NotebookLM&lt;/p&gt;
&lt;h2 id="the-two-phases-of-llm-inference"&gt;The Two Phases of LLM Inference&lt;/h2&gt;
&lt;p&gt;Every time you send a prompt to an LLM and get a response, two distinct computational phases occur.&lt;/p&gt;
&lt;h3 id="phase-1-prefill-prompt-evaluation"&gt;Phase 1: Prefill (Prompt Evaluation)&lt;/h3&gt;
&lt;p&gt;During prefill, the model processes all input tokens (your system prompt, conversation history, tool definitions, and the new user message) in parallel via batched matrix multiplications.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Key characteristics:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Compute-bound&lt;/strong&gt;: The bottleneck is raw computation, not memory bandwidth&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Parallel&lt;/strong&gt;: All input tokens are processed simultaneously (within batch limits)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Scales with&lt;/strong&gt;: &lt;code&gt;batch_size × seq_len × hidden_dim&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Measured in&lt;/strong&gt;: tokens/second of input processed&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For our qwen3-coder-next model, prompt evaluation runs at ~20 tokens/second. That means evaluating a 16K token prompt takes approximately 800 seconds (~13 minutes) without KV cache reuse.&lt;/p&gt;
&lt;p&gt;Why is it faster per-token than decode? Because the GPU can parallelize the matrix multiplications across all input tokens simultaneously. But &amp;ldquo;faster per-token&amp;rdquo; doesn&amp;rsquo;t mean &amp;ldquo;fast&amp;rdquo;. When you have 16,000+ tokens of input, the absolute time is still significant.&lt;/p&gt;
&lt;h3 id="phase-2-decode-token-generation"&gt;Phase 2: Decode (Token Generation)&lt;/h3&gt;
&lt;p&gt;During decode, the model generates output one token at a time. Each new token is produced autoregressively. It depends on every token that came before it, so there&amp;rsquo;s no way to parallelize the generation itself.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Key characteristics:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Memory-bandwidth-bound&lt;/strong&gt;: The bottleneck is reading the KV cache and model weights from memory for each token&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Sequential&lt;/strong&gt;: Strictly one token at a time&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Scales with&lt;/strong&gt;: model size + KV cache size (memory reads per token)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Measured in&lt;/strong&gt;: tokens/second of output generated&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For our model, decode runs at ~23 tokens/second. The limiting factor is reading ~54 GB of model weights from unified memory for each token, plus the KV cache. Apple Silicon&amp;rsquo;s unified memory bandwidth (M4 Pro: ~273 GB/s) determines this ceiling.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;The key insight&lt;/strong&gt;: In an agentic workflow like Claude Code with 57 API round-trips, most time is spent in prefill, not decode. Decode only happens once per round-trip (for the response), but prefill happens every time with the full conversation context.&lt;/p&gt;
&lt;h2 id="how-the-kv-cache-works"&gt;How the KV Cache Works&lt;/h2&gt;
&lt;p&gt;The KV cache is the single most important concept for understanding local LLM performance.&lt;/p&gt;
&lt;h3 id="what-it-stores"&gt;What It Stores&lt;/h3&gt;
&lt;p&gt;During the attention computation in each transformer layer, the model computes three projections of the input: &lt;strong&gt;Query (Q)&lt;/strong&gt;, &lt;strong&gt;Key (K)&lt;/strong&gt;, and &lt;strong&gt;Value (V)&lt;/strong&gt;. The Q projection is used immediately, but K and V are stored in the KV cache for future reference.&lt;/p&gt;
&lt;p&gt;Each layer independently maintains its own K and V tensors. The KV cache stores, for every token in the current sequence, the Key and Value vectors for every layer and every attention head.&lt;/p&gt;
&lt;h3 id="why-it-exists"&gt;Why It Exists&lt;/h3&gt;
&lt;p&gt;Without the KV cache, generating token N+1 would require recomputing the attention over all N previous tokens from scratch, re-running the full model forward pass for the entire sequence. With the KV cache, the model only needs to:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Compute Q, K, V for the new token&lt;/li&gt;
&lt;li&gt;Append the new K, V to the cache&lt;/li&gt;
&lt;li&gt;Compute attention between the new Q and all cached K, V entries&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This turns an O(n²) per-token decode cost into O(n), at the expense of memory.&lt;/p&gt;
&lt;h3 id="memory-formula"&gt;Memory Formula&lt;/h3&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;KV Cache Memory = 2 × num_layers × num_kv_heads × head_dim × seq_len × dtype_bytes
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Where:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;2&lt;/code&gt;: one for Keys, one for Values&lt;/li&gt;
&lt;li&gt;&lt;code&gt;num_layers&lt;/code&gt;: number of transformer layers (48 for qwen3-coder-next)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;num_kv_heads&lt;/code&gt;: number of key/value attention heads (may differ from query heads in GQA models)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;head_dim&lt;/code&gt;: dimension per attention head (typically &lt;code&gt;hidden_dim / num_heads&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;seq_len&lt;/code&gt;: current sequence length (up to &lt;code&gt;num_ctx&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;dtype_bytes&lt;/code&gt;: bytes per element (2 for fp16, 1 for q8_0, 0.5 for q4_0)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="worked-example-qwen3-coder-next"&gt;Worked Example: qwen3-coder-next&lt;/h3&gt;
&lt;p&gt;The model has these attention parameters:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Layers&lt;/strong&gt;: 48&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Attention heads&lt;/strong&gt;: 16&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Embedding dimension&lt;/strong&gt;: 2048&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Head dimension&lt;/strong&gt;: 2048 / 16 = 128&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;At 16K context (num_ctx = 16,384) with fp16:&lt;/strong&gt;&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;KV Cache = 2 × 48 × 16 × 128 × 16,384 × 2 bytes
= 2 × 48 × 16 × 128 × 16,384 × 2
= 6,442,450,944 bytes
≈ 6.0 GB
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;At 32K context (num_ctx = 32,768) with fp16:&lt;/strong&gt;&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;KV Cache = 2 × 48 × 16 × 128 × 32,768 × 2 bytes
≈ 12.0 GB
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Doubling the context length doubles the KV cache memory. With a 54 GB model in a 64 GB system, 6 GB for the KV cache at 16K context is tight but feasible. 12 GB at 32K would push total memory usage to 66 GB, exceeding physical RAM and triggering swap.&lt;/p&gt;
&lt;h3 id="kv-cache-quantization"&gt;KV Cache Quantization&lt;/h3&gt;
&lt;p&gt;This is where &lt;code&gt;OLLAMA_KV_CACHE_TYPE&lt;/code&gt; comes in. Instead of storing K and V in fp16 (2 bytes per element), you can quantize them:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;q8_0 (8-bit quantization):&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;1 byte per element instead of 2&lt;/li&gt;
&lt;li&gt;~50% memory reduction&lt;/li&gt;
&lt;li&gt;Minimal quality loss in practice&lt;/li&gt;
&lt;li&gt;16K context: ~3.0 GB instead of 6.0 GB&lt;/li&gt;
&lt;li&gt;32K context: ~6.0 GB instead of 12.0 GB, now fits comfortably alongside the 54 GB model&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;q4_0 (4-bit quantization):&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;0.5 bytes per element&lt;/li&gt;
&lt;li&gt;~75% memory reduction&lt;/li&gt;
&lt;li&gt;More quality risk; attention patterns may degrade on long sequences&lt;/li&gt;
&lt;li&gt;16K context: ~1.5 GB&lt;/li&gt;
&lt;li&gt;32K context: ~3.0 GB&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For a 64 GB system with a 54 GB model, &lt;strong&gt;q8_0 is the sweet spot&lt;/strong&gt;. It halves KV cache memory with negligible quality impact, giving you headroom to increase context length.&lt;/p&gt;
&lt;h3 id="kv-cache-reuse-in-ollama"&gt;KV Cache Reuse in Ollama&lt;/h3&gt;
&lt;p&gt;Ollama implements an important optimization: &lt;strong&gt;prompt prefix caching&lt;/strong&gt;. If a new request shares the same prefix as the previous request (which is the normal case in a multi-turn conversation, since the history stays the same and you just append a new user message), Ollama reuses the KV cache entries for the shared prefix and only evaluates the new tokens at the end.&lt;/p&gt;
&lt;p&gt;This is why the second API call in my debug trace took only 6 seconds instead of 77: the KV cache from the first call was still warm, and only the new ~200 tokens needed evaluation.&lt;/p&gt;
&lt;p&gt;For this to work:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;The model must stay loaded in memory (set &lt;code&gt;OLLAMA_KEEP_ALIVE=60m&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;The context must not have been evicted (happens if context exceeds &lt;code&gt;num_ctx&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;The prompt prefix must be byte-identical to the previous request&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="attention-and-flash-attention"&gt;Attention and Flash Attention&lt;/h2&gt;
&lt;h3 id="standard-vanilla-attention"&gt;Standard (Vanilla) Attention&lt;/h3&gt;
&lt;p&gt;The core attention computation is:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;Attention(Q, K, V) = softmax(Q × K^T / √d_k) × V
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Where:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Q&lt;/code&gt; = Query matrix (seq_len × head_dim)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;K&lt;/code&gt; = Key matrix (seq_len × head_dim)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;V&lt;/code&gt; = Value matrix (seq_len × head_dim)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;d_k&lt;/code&gt; = head dimension (for scaling)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The expensive step is &lt;code&gt;Q × K^T&lt;/code&gt;, which produces the &lt;strong&gt;attention score matrix&lt;/strong&gt; of shape (seq_len × seq_len). This is the O(n²) bottleneck.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Memory cost for the attention matrix alone:&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;For a 32K context with fp16:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;32,768 × 32,768 × 2 bytes = 2,147,483,648 bytes ≈ 2.0 GB
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;That&amp;rsquo;s 2 GB &lt;strong&gt;per attention head, per layer&lt;/strong&gt;. With 16 heads and 48 layers, the total attention memory would be astronomical. In practice, implementations process one head at a time, but the O(n²) memory for a single head&amp;rsquo;s attention matrix is still the constraint.&lt;/p&gt;
&lt;h3 id="flash-attention-tiled-computation"&gt;Flash Attention: Tiled Computation&lt;/h3&gt;
&lt;p&gt;Flash Attention, introduced by Dao et al. (2022), changes how attention is computed by never materializing the full n×n attention matrix.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Key ideas:&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Tiling&lt;/strong&gt;: Instead of computing the full attention matrix, Flash Attention breaks Q, K, V into blocks that fit in fast on-chip SRAM (shared memory / register file)&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Online softmax&lt;/strong&gt;: Uses an online algorithm to compute the softmax incrementally across tiles, without needing the full row of attention scores&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;IO-awareness&lt;/strong&gt;: Minimizes data movement between slow main memory (unified memory / HBM) and fast SRAM. The standard attention algorithm reads/writes the full n×n matrix to main memory; Flash Attention keeps intermediate values in SRAM&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Memory complexity&lt;/strong&gt;: O(n) instead of O(n²). Only needs to store Q, K, V and the output, never the full attention matrix&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Speed improvement&lt;/strong&gt;: Counterintuitively, Flash Attention is also &lt;em&gt;faster&lt;/em&gt; despite doing the same mathematical computation. By reducing memory I/O (which is the actual bottleneck on modern hardware), it achieves higher throughput.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;On Apple Silicon&lt;/strong&gt;: Flash Attention is implemented via Metal compute shaders. Apple Silicon&amp;rsquo;s unified memory architecture (where CPU and GPU share the same physical memory) makes Flash Attention especially effective because there&amp;rsquo;s no PCIe transfer overhead. The GPU compute units can directly access model weights and KV cache in unified memory, and Flash Attention ensures the attention computation stays in fast on-chip memory.&lt;/p&gt;
&lt;p&gt;Enable it in Ollama with: &lt;code&gt;OLLAMA_FLASH_ATTENTION=1&lt;/code&gt;&lt;/p&gt;
&lt;h2 id="context-length-and-its-cost"&gt;Context Length and Its Cost&lt;/h2&gt;
&lt;p&gt;The &lt;code&gt;num_ctx&lt;/code&gt; parameter in Ollama controls how many tokens the model can consider in a single request. It has two direct costs:&lt;/p&gt;
&lt;h3 id="linear-cost-kv-cache-memory"&gt;Linear Cost: KV Cache Memory&lt;/h3&gt;
&lt;p&gt;As shown in the KV cache calculation above, doubling context length doubles KV cache memory. This is a linear relationship, straightforward and predictable.&lt;/p&gt;
&lt;h3 id="quadratic-cost-attention-without-flash-attention"&gt;Quadratic Cost: Attention (Without Flash Attention)&lt;/h3&gt;
&lt;p&gt;Without Flash Attention, attention computation scales with O(n²) in both memory and compute. Going from 16K to 32K context quadruples the attention matrix size. Flash Attention reduces the memory cost to O(n), but the compute cost of attention is still O(n² × d), since every query must attend to every key. What Flash Attention eliminates is the &lt;em&gt;memory materialization&lt;/em&gt;, not the mathematical operations.&lt;/p&gt;
&lt;h3 id="the-num_ctx-pre-allocation"&gt;The num_ctx Pre-allocation&lt;/h3&gt;
&lt;p&gt;When Ollama loads a model with a given &lt;code&gt;num_ctx&lt;/code&gt;, it &lt;strong&gt;pre-allocates&lt;/strong&gt; the full KV cache for that many tokens upfront. Even if your prompt is only 1,000 tokens, the memory for 16,384 tokens (or whatever &lt;code&gt;num_ctx&lt;/code&gt; is set to) is already reserved.&lt;/p&gt;
&lt;p&gt;This means increasing &lt;code&gt;num_ctx&lt;/code&gt; has an immediate memory cost at model load time, not just when the context is actually filled. Plan your &lt;code&gt;num_ctx&lt;/code&gt; based on available memory:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;Available for KV cache = Total RAM - Model size - OS overhead
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;For our setup:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;64 GB - 54 GB (model) - 4 GB (OS/apps) = ~6 GB available
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;With q8_0 KV cache: 6 GB supports ~32K context. With fp16 KV cache: 6 GB supports ~16K context.&lt;/p&gt;
&lt;h2 id="batch-size-num_batch-and-prompt-eval-speed"&gt;Batch Size (num_batch) and Prompt Eval Speed&lt;/h2&gt;
&lt;p&gt;The &lt;code&gt;num_batch&lt;/code&gt; parameter controls how many tokens are processed in a single forward pass during prefill. This directly affects prompt evaluation speed.&lt;/p&gt;
&lt;h3 id="how-it-works"&gt;How It Works&lt;/h3&gt;
&lt;p&gt;During prefill, if you have 16,000 input tokens and &lt;code&gt;num_batch=512&lt;/code&gt; (the default), the model performs 32 forward passes (16,000 ÷ 512). With &lt;code&gt;num_batch=2048&lt;/code&gt;, it performs 8 forward passes, 4x fewer iterations.&lt;/p&gt;
&lt;h3 id="why-larger-batches-are-faster"&gt;Why Larger Batches Are Faster&lt;/h3&gt;
&lt;p&gt;GPU matrix multiplication is most efficient when matrices are large enough to saturate the compute units. A batch of 512 tokens may not fully use the GPU&amp;rsquo;s parallel capacity, especially on Apple Silicon with its wide SIMD units. A batch of 2048 better saturates the hardware.&lt;/p&gt;
&lt;h3 id="real-world-impact"&gt;Real-World Impact&lt;/h3&gt;
&lt;p&gt;The improvement isn&amp;rsquo;t a perfect linear scaling (2048/512 = 4x) because other factors like memory bandwidth and cache effects come into play, but a &lt;strong&gt;2-3x speedup in prompt evaluation&lt;/strong&gt; from increasing batch size from 512 to 2048 is typical.&lt;/p&gt;
&lt;h3 id="memory-trade-off"&gt;Memory Trade-Off&lt;/h3&gt;
&lt;p&gt;Larger batches require more memory for intermediate activations (the temporary tensors computed during the forward pass). For a model that&amp;rsquo;s already using 54 GB, the additional memory for 2048-token batches is modest (usually a few hundred MB) but worth monitoring on memory-constrained systems.&lt;/p&gt;
&lt;p&gt;Set it in Ollama: &lt;code&gt;OLLAMA_NUM_BATCH=2048&lt;/code&gt;&lt;/p&gt;
&lt;h2 id="inference-engines-for-macos-apple-silicon"&gt;Inference Engines for macOS Apple Silicon&lt;/h2&gt;
&lt;p&gt;Not all inference engines are equal on Apple Silicon. Here&amp;rsquo;s a technical comparison.&lt;/p&gt;
&lt;h3 id="ollama"&gt;Ollama&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Backend&lt;/strong&gt;: llama.cpp compiled with Metal acceleration, wrapped in a Go server&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Strengths&lt;/strong&gt;: Easiest setup, built-in model management, KV cache reuse between requests, Anthropic/OpenAI-compatible API endpoints, automatic GGUF model downloading&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Limitations&lt;/strong&gt;: Less control over advanced parameters (no per-layer GPU offloading control, limited quantization options for KV cache beyond what env vars expose)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Best for&lt;/strong&gt;: General use, Claude Code integration, getting started quickly&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="llamacpp-direct"&gt;llama.cpp (Direct)&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Backend&lt;/strong&gt;: The C/C++ engine that powers Ollama, used directly via &lt;code&gt;llama-server&lt;/code&gt; or &lt;code&gt;llama-cli&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Strengths&lt;/strong&gt;: Full parameter control (every runtime flag exposed), latest features land here first before Ollama adopts them, &lt;code&gt;llama-server&lt;/code&gt; provides an OpenAI-compatible API, supports advanced features like speculative decoding and grammar-constrained generation&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Limitations&lt;/strong&gt;: Manual model file management, must compile from source for latest features, no built-in model downloading&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Best for&lt;/strong&gt;: Power users who want maximum control over inference parameters&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="mlx--mlx-lm"&gt;MLX / mlx-lm&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Backend&lt;/strong&gt;: Apple&amp;rsquo;s own ML framework with native Metal optimization&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Strengths&lt;/strong&gt;: Designed specifically for Apple Silicon with optimized memory access patterns for unified memory, native quantization support, potentially fastest inference on Mac hardware. Avoids the overhead of llama.cpp&amp;rsquo;s Metal backend (which is a cross-platform abstraction)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Limitations&lt;/strong&gt;: Requires models in MLX format (safetensors with MLX-specific quantization), smaller model ecosystem, no built-in Anthropic-compatible API (needs a proxy)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Best for&lt;/strong&gt;: Getting maximum performance from Apple Silicon, especially for prompt evaluation where MLX&amp;rsquo;s optimized memory access shines&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="lm-studio"&gt;LM Studio&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Backend&lt;/strong&gt;: Same llama.cpp engine as Ollama, with a GUI wrapper and OpenAI-compatible API server&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Strengths&lt;/strong&gt;: User-friendly interface, good for experimentation and model comparison, easy API server setup&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Limitations&lt;/strong&gt;: No fundamental speed advantage over Ollama (same backend), less suited for headless/CLI workflows&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Best for&lt;/strong&gt;: Users who prefer a visual interface for model management and testing&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="vllm"&gt;vLLM&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Backend&lt;/strong&gt;: Purpose-built for NVIDIA CUDA GPUs with features like PagedAttention, continuous batching, and tensor parallelism&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Strengths&lt;/strong&gt;: State-of-the-art throughput on NVIDIA hardware, excellent for serving multiple concurrent users&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Critical limitation on Mac&lt;/strong&gt;: Falls back to CPU-only mode since there&amp;rsquo;s no CUDA on Apple Silicon. Performance drops to &lt;strong&gt;~1-2 tokens/second&lt;/strong&gt;, unusable for interactive work&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Best for&lt;/strong&gt;: NVIDIA GPU servers exclusively. Do not use on Mac.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="comparison-table"&gt;Comparison Table&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Engine&lt;/th&gt;
&lt;th&gt;Backend&lt;/th&gt;
&lt;th&gt;Apple Silicon Support&lt;/th&gt;
&lt;th&gt;Prompt Eval Speed&lt;/th&gt;
&lt;th&gt;Ease of Use&lt;/th&gt;
&lt;th&gt;API Compatibility&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;Ollama&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;llama.cpp + Metal&lt;/td&gt;
&lt;td&gt;Good&lt;/td&gt;
&lt;td&gt;Good&lt;/td&gt;
&lt;td&gt;Excellent&lt;/td&gt;
&lt;td&gt;Anthropic + OpenAI&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;llama.cpp&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;Native + Metal&lt;/td&gt;
&lt;td&gt;Good&lt;/td&gt;
&lt;td&gt;Good&lt;/td&gt;
&lt;td&gt;Moderate&lt;/td&gt;
&lt;td&gt;OpenAI&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;MLX&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;Apple Metal native&lt;/td&gt;
&lt;td&gt;Best&lt;/td&gt;
&lt;td&gt;Best (potential)&lt;/td&gt;
&lt;td&gt;Moderate&lt;/td&gt;
&lt;td&gt;Needs proxy&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;LM Studio&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;llama.cpp + Metal&lt;/td&gt;
&lt;td&gt;Good&lt;/td&gt;
&lt;td&gt;Good&lt;/td&gt;
&lt;td&gt;Excellent&lt;/td&gt;
&lt;td&gt;OpenAI&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;vLLM&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;CUDA (CPU fallback)&lt;/td&gt;
&lt;td&gt;None (CPU only)&lt;/td&gt;
&lt;td&gt;Unusable&lt;/td&gt;
&lt;td&gt;Moderate&lt;/td&gt;
&lt;td&gt;OpenAI&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2 id="factors-that-affect-inference-speed"&gt;Factors That Affect Inference Speed&lt;/h2&gt;
&lt;p&gt;To summarize all the variables that determine how fast your local LLM runs:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Model factors:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Model size&lt;/strong&gt;: Larger models require more memory bandwidth per token. A 79.7B model at Q4_K_M (~54 GB) pushes the limits of 64 GB unified memory&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Quantization level&lt;/strong&gt;: Lower quantization (Q4 vs Q8 vs FP16) reduces memory and speeds up inference at a quality cost&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Number of layers and attention heads&lt;/strong&gt;: More layers = more sequential computation; more heads = more attention computation per layer&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Configuration factors:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Context length (num_ctx)&lt;/strong&gt;: Larger context = more KV cache memory + longer prefill&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Batch size (num_batch)&lt;/strong&gt;: Larger batches = faster prompt evaluation (up to hardware limits)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Flash Attention&lt;/strong&gt;: Reduces attention memory from O(n²) to O(n), speeds up long-context inference&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;KV cache quantization&lt;/strong&gt;: q8_0 halves cache memory, enabling larger context or more headroom&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Hardware factors:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Memory bandwidth&lt;/strong&gt;: The primary bottleneck for token generation. Apple M4 Pro: ~273 GB/s&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Unified memory size&lt;/strong&gt;: Must fit model + KV cache + OS. Running out triggers swap, which kills performance&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;GPU compute units&lt;/strong&gt;: Affects prefill speed (compute-bound). M4 Pro has a capable GPU but can&amp;rsquo;t match dedicated NVIDIA datacenter GPUs&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Workflow factors:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Number of API round-trips&lt;/strong&gt;: Each round-trip pays a prefill cost. Agentic workflows (like Claude Code) make many round-trips&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Prompt caching / KV cache reuse&lt;/strong&gt;: Significantly reduces prefill cost for conversational workloads where the prefix is shared&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Context management&lt;/strong&gt;: Keeping the active context small (via autocompaction or summarization) reduces prefill time proportionally&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Understanding these factors lets you make informed trade-offs. For a 79.7B model on 64 GB Apple Silicon, the optimal configuration is: Q4_K_M model quantization, q8_0 KV cache, Flash Attention enabled, 16–32K context, batch size 2048, and aggressive context management on the client side.&lt;/p&gt;
&lt;h2 id="follow-me"&gt;Follow me&lt;/h2&gt;
&lt;p&gt;If you are new to my posts, I regularly post about AI, LLMs, AWS, EKS, Kubernetes and Cloud computing related topics. Do follow me in &lt;a href="https://www.linkedin.com/in/vijaykodam/"&gt;LinkedIn&lt;/a&gt; and visit &lt;a href="https://dev.to/vijaykodam"&gt;my dev.to posts&lt;/a&gt;. You can find all my previous blog posts in &lt;a href="https://vijay.eu/posts"&gt;my blog&lt;/a&gt;&lt;/p&gt;</description></item><item><title>Why Claude Code with Ollama took 1 hour instead of 9 minutes, and how I fixed it</title><link>https://vijay.eu/co-authored/ollama-claude-code-performance-guide/</link><pubDate>Sat, 14 Feb 2026 18:00:00 +0300</pubDate><guid>https://vijay.eu/co-authored/ollama-claude-code-performance-guide/</guid><description>&lt;h2 id="the-dream-setup"&gt;The Dream Setup&lt;/h2&gt;
&lt;p&gt;I finally got Claude Code running with my own local LLM. Everything local, everything private, running on my Mac Mini M4 Pro. No API keys, no cloud costs, no data leaving my network. Just me, my hardware, and a 79.7 billion parameter coding model.&lt;/p&gt;
&lt;p&gt;But something was very wrong. A simple task (scaffolding a Django REST API with JWT authentication) took &lt;strong&gt;1 hour&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;At 23 tokens per second, the ~13,000 tokens of output should have taken about &lt;strong&gt;9 minutes&lt;/strong&gt;. Where did the other 51 minutes go?&lt;/p&gt;
&lt;p&gt;This post walks through how I diagnosed the problem, what I learned about how LLM inference actually works, and the specific fixes that brought the time down significantly. If you&amp;rsquo;re running Claude Code with Ollama (or any local LLM), this will save you a lot of frustration.&lt;/p&gt;
&lt;h2 id="my-setup"&gt;My Setup&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Hardware&lt;/strong&gt;: Mac Mini M4 Pro, 64GB unified memory&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Model&lt;/strong&gt;: &lt;code&gt;qwen3-coder-next:q4_K_M&lt;/code&gt;, a 79.7B parameter coding model, quantized to Q4_K_M (~48 GB on disk, ~54 GB in GPU memory)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Inference server&lt;/strong&gt;: Ollama, running on the Mac Mini&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Client&lt;/strong&gt;: Claude Code v2.1.42, connecting to Ollama from another Mac on the local network&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Launch command&lt;/strong&gt;:
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;ANTHROPIC_AUTH_TOKEN&lt;span style="color:#f92672"&gt;=&lt;/span&gt;ollama &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;ANTHROPIC_BASE_URL&lt;span style="color:#f92672"&gt;=&lt;/span&gt;http://192.168.10.1:11434 &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;ANTHROPIC_API_KEY&lt;span style="color:#f92672"&gt;=&lt;/span&gt;&lt;span style="color:#e6db74"&gt;&amp;#34;&amp;#34;&lt;/span&gt; &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;claude --model qwen3-coder-next:q4_K_M
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Raw benchmarks looked promising. When I tested the model directly via Ollama&amp;rsquo;s API:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Token generation&lt;/strong&gt;: ~23 tokens/second&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Prompt evaluation&lt;/strong&gt;: ~20 tokens/second&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Those numbers aren&amp;rsquo;t blazing fast, but they&amp;rsquo;re perfectly usable. So why was the full Claude Code session so painfully slow?&lt;/p&gt;
&lt;h2 id="the-problem"&gt;The Problem&lt;/h2&gt;
&lt;p&gt;The numbers told a clear story:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Metric&lt;/th&gt;
&lt;th&gt;Value&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Total wall time&lt;/td&gt;
&lt;td&gt;~66 minutes&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Output tokens&lt;/td&gt;
&lt;td&gt;~13,000&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Generation speed&lt;/td&gt;
&lt;td&gt;23 tok/s&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Expected generation time&lt;/td&gt;
&lt;td&gt;~9 minutes&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;Unexplained overhead&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;~57 minutes&lt;/strong&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Nearly 90% of the time was spent doing something other than generating tokens. But what?&lt;/p&gt;
&lt;h2 id="how-i-found-the-bottleneck"&gt;How I Found the Bottleneck&lt;/h2&gt;
&lt;p&gt;Claude Code writes debug logs to &lt;code&gt;~/.claude/debug/&lt;/code&gt;. Each session gets its own log file. I opened up the log for my slow session (&lt;code&gt;f519f739-d0f9-41b1-ab4c-a7e01a0e447e.txt&lt;/code&gt;, 395 KB of trace data) and started looking at timestamps.&lt;/p&gt;
&lt;p&gt;The key was comparing two events for each API call:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;API request sent&lt;/strong&gt;: when Claude Code sends a request to Ollama&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Stream started&lt;/strong&gt;: when the first token comes back&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The gap between these two timestamps is the &lt;strong&gt;prompt evaluation time&lt;/strong&gt;, the model &amp;ldquo;reading&amp;rdquo; all the input before it can start &amp;ldquo;writing&amp;rdquo; a response.&lt;/p&gt;
&lt;p&gt;Here&amp;rsquo;s what I found:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Request&lt;/th&gt;
&lt;th&gt;Wait Time&lt;/th&gt;
&lt;th&gt;Input Tokens&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;First call (cold start)&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;77 seconds&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;121&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Call at 17K tokens&lt;/td&gt;
&lt;td&gt;15 seconds&lt;/td&gt;
&lt;td&gt;17,173&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Mid-session&lt;/td&gt;
&lt;td&gt;66 seconds&lt;/td&gt;
&lt;td&gt;~25,000&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Late session&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;132 seconds&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;~30,000&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Near the end&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;194 seconds&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;~34,566&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The wait times were growing because the input context was growing, and it was growing far beyond what the model could actually handle. More on that in a moment.&lt;/p&gt;
&lt;p&gt;With &lt;strong&gt;57 API round-trips&lt;/strong&gt; in a single session, each one incurring 15–194 seconds of prompt evaluation overhead, the math becomes obvious: most of the hour was spent waiting for the model to re-read the conversation history before generating each response.&lt;/p&gt;
&lt;p&gt;&lt;img src="https://vijay.eu/images/optimizing-local-inference.webp" alt="optimizingllms"&gt;&lt;/p&gt;
&lt;p&gt;Credits: Generated using NotebookLM&lt;/p&gt;
&lt;h2 id="understanding-what-happens-during-inference"&gt;Understanding What Happens During Inference&lt;/h2&gt;
&lt;p&gt;Before getting into the fixes, it helps to understand what happens when a local LLM processes your request. I&amp;rsquo;ll use analogies here. For the full technical breakdown with formulas and memory calculations, see my companion post: &lt;a href="https://vijay.eu/co-authored/llm-inference-internals-apple-silicon/"&gt;LLM Inference Internals: KV Cache, Flash Attention, and Optimizing for Apple Silicon&lt;/a&gt;.&lt;/p&gt;
&lt;h3 id="two-phases-reading-vs-writing"&gt;Two Phases: Reading vs Writing&lt;/h3&gt;
&lt;p&gt;LLM inference has two distinct phases:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Prompt evaluation (prefill)&lt;/strong&gt;: The model reads the entire input (system prompt, conversation history, tool definitions, your latest message). This is like reading an entire book before you can answer a question about it. It processes all tokens in parallel, but it still takes time proportional to how much there is to read.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Token generation (decode)&lt;/strong&gt;: The model writes its response, one token at a time. This is the &amp;ldquo;thinking and typing&amp;rdquo; phase. Each new token depends on everything that came before, so it can only produce one at a time.&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The generation speed (23 tok/s) is what you see as the response streams in. The prompt evaluation (20 tok/s) happens silently before any text appears. It&amp;rsquo;s the &amp;ldquo;thinking pause&amp;rdquo; before the model starts responding.&lt;/p&gt;
&lt;h3 id="the-kv-cache-bookmarks-for-the-model"&gt;The KV Cache: Bookmarks for the Model&lt;/h3&gt;
&lt;p&gt;When the model reads your input during prompt evaluation, it computes something called the &lt;strong&gt;KV cache&lt;/strong&gt; (Key-Value cache). Think of these as bookmarks. They capture what the model &amp;ldquo;understood&amp;rdquo; about each part of the text, so it doesn&amp;rsquo;t have to re-read everything for every single token it generates.&lt;/p&gt;
&lt;p&gt;Without the KV cache, generating a 500-token response would require re-reading the entire input 500 times. With it, the model reads once and then references its bookmarks.&lt;/p&gt;
&lt;p&gt;The catch: the KV cache takes memory, and it grows with the length of the conversation. A 16K token context needs significantly less KV cache memory than a 32K token context.&lt;/p&gt;
&lt;h3 id="context-window-the-models-reading-capacity"&gt;Context Window: The Model&amp;rsquo;s Reading Capacity&lt;/h3&gt;
&lt;p&gt;The &lt;strong&gt;context window&lt;/strong&gt; is how much total text the model can consider at once (input and output combined). It&amp;rsquo;s like the size of the desk the model is working at. Bigger desk means more documents can be open at once, but it also means more memory used.&lt;/p&gt;
&lt;p&gt;My model&amp;rsquo;s native context length is 262K tokens, but Ollama was configured with a 16K context window (&lt;code&gt;num_ctx: 16384&lt;/code&gt;). That&amp;rsquo;s the actual working size.&lt;/p&gt;
&lt;h3 id="flash-attention-speed-reading"&gt;Flash Attention: Speed-Reading&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Flash Attention&lt;/strong&gt; is an optimization for how the model processes attention (the mechanism that relates different parts of the text to each other). Standard attention requires memory proportional to the &lt;em&gt;square&lt;/em&gt; of the sequence length. Flash Attention brings that down to linear by processing the attention in small tiles rather than all at once.&lt;/p&gt;
&lt;p&gt;Think of it as the difference between spreading out every page of a book on the floor to compare them all simultaneously (standard attention) versus reading through the book chapter by chapter, keeping only the current chapter in front of you (Flash Attention).&lt;/p&gt;
&lt;h3 id="autocompaction-summarizing-old-messages"&gt;Autocompaction: Summarizing Old Messages&lt;/h3&gt;
&lt;p&gt;Claude Code has a built-in mechanism called &lt;strong&gt;autocompaction&lt;/strong&gt;. When the conversation gets too long for the context window, it summarizes older messages to free up space. This keeps the conversation going without hitting the context limit.&lt;/p&gt;
&lt;p&gt;The trigger point is based on what Claude Code thinks the context window size is. This turns out to be very important, as we&amp;rsquo;ll see.&lt;/p&gt;
&lt;h3 id="prompt-caching-reusing-the-bookmarks"&gt;Prompt Caching: Reusing the Bookmarks&lt;/h3&gt;
&lt;p&gt;When Ollama receives a new request where the beginning of the prompt is identical to the previous request (which is typical in a conversation, since the history stays the same and you just add a new message at the end), it can &lt;strong&gt;reuse the KV cache&lt;/strong&gt; from the previous request. Only the new tokens at the end need to be evaluated.&lt;/p&gt;
&lt;p&gt;This is a big optimization, but only works if the model stays loaded in memory and the context hasn&amp;rsquo;t been evicted.&lt;/p&gt;
&lt;h2 id="the-five-root-causes"&gt;The Five Root Causes&lt;/h2&gt;
&lt;p&gt;Armed with an understanding of inference, I could now identify exactly what was going wrong.&lt;/p&gt;
&lt;h3 id="1-prompt-evaluation-dominated-every-request"&gt;1. Prompt Evaluation Dominated Every Request&lt;/h3&gt;
&lt;p&gt;Each of the 57 API round-trips paid a prompt evaluation cost. Even with KV cache reuse, the model had to evaluate the new portion of the prompt each time. As the conversation grew, these costs escalated from 6 seconds to over 3 minutes per request.&lt;/p&gt;
&lt;p&gt;Total prompt evaluation time across 57 calls: &lt;strong&gt;~50 minutes&lt;/strong&gt; out of the 66-minute session.&lt;/p&gt;
&lt;h3 id="2-context-window-mismatch"&gt;2. Context Window Mismatch&lt;/h3&gt;
&lt;p&gt;This was the biggest problem. Claude Code was configured to think the model had a &lt;strong&gt;180,000 token&lt;/strong&gt; context window (its default assumption for Claude models). The autocompaction threshold was set at 167,000 tokens:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;autocompact: tokens=34566 threshold=167000 effectiveWindow=180000
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;But Ollama&amp;rsquo;s actual context window was only &lt;strong&gt;16,384 tokens&lt;/strong&gt;. This meant:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Claude Code never triggered autocompaction (it thought there was tons of room)&lt;/li&gt;
&lt;li&gt;The conversation context ballooned to 34,566 tokens, more than &lt;strong&gt;2x&lt;/strong&gt; what Ollama could actually fit&lt;/li&gt;
&lt;li&gt;Ollama was silently truncating or struggling with context that exceeded its window&lt;/li&gt;
&lt;li&gt;Each prompt evaluation was processing far more tokens than necessary&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="3-fifty-seven-api-round-trips"&gt;3. Fifty-Seven API Round-Trips&lt;/h3&gt;
&lt;p&gt;Claude Code is an agentic system. It doesn&amp;rsquo;t just answer once. It plans, calls tools (file reads, writes, bash commands), observes results, and iterates. For my Django project, this meant 57 separate API calls in one session. Each one triggered a new prompt evaluation cycle.&lt;/p&gt;
&lt;p&gt;This is normal behavior for Claude Code, but it means prompt evaluation speed is the dominant factor in session time, not token generation speed.&lt;/p&gt;
&lt;h3 id="4-failed-haiku-calls"&gt;4. Failed Haiku Calls&lt;/h3&gt;
&lt;p&gt;After nearly every API call, Claude Code tried to count tokens by calling &lt;code&gt;claude-haiku-4-5-20251001&lt;/code&gt;, a cheap, fast model used for token counting in the normal Claude API setup. But this model doesn&amp;rsquo;t exist in Ollama:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;countTokensWithFallback: haiku fallback failed: 404
{&amp;#34;type&amp;#34;:&amp;#34;error&amp;#34;,&amp;#34;error&amp;#34;:{&amp;#34;type&amp;#34;:&amp;#34;not_found_error&amp;#34;,
&amp;#34;message&amp;#34;:&amp;#34;model &amp;#39;claude-haiku-4-5-20251001&amp;#39; not found&amp;#34;}}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Each failed call involved a streaming attempt (404), then a non-streaming fallback (also 404). That&amp;rsquo;s two wasted HTTP round-trips per token counting attempt, happening dozens of times per session.&lt;/p&gt;
&lt;h3 id="5-streaming-404-fallback-pattern"&gt;5. Streaming 404 Fallback Pattern&lt;/h3&gt;
&lt;p&gt;For each haiku call, the sequence was:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Try streaming endpoint → 404&lt;/li&gt;
&lt;li&gt;Fall back to non-streaming endpoint → also 404&lt;/li&gt;
&lt;li&gt;Give up and continue without token count&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This wasn&amp;rsquo;t a big deal on its own, but multiplied across 57+ attempts, it added up to unnecessary network overhead and wasted time.&lt;/p&gt;
&lt;h2 id="the-fixes"&gt;The Fixes&lt;/h2&gt;
&lt;h3 id="ollama-server-optimizations"&gt;Ollama Server Optimizations&lt;/h3&gt;
&lt;p&gt;If you&amp;rsquo;re running the Ollama Mac app, set these environment variables using &lt;code&gt;launchctl setenv&lt;/code&gt; (and create a LaunchAgent plist to make them persist across reboots):&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;launchctl setenv OLLAMA_FLASH_ATTENTION &lt;span style="color:#ae81ff"&gt;1&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;launchctl setenv OLLAMA_KV_CACHE_TYPE q8_0
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;launchctl setenv OLLAMA_KEEP_ALIVE 60m
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;launchctl setenv OLLAMA_NUM_BATCH &lt;span style="color:#ae81ff"&gt;2048&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;If you&amp;rsquo;re running &lt;code&gt;ollama serve&lt;/code&gt; from the command line instead, set them as environment variables:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;OLLAMA_FLASH_ATTENTION&lt;span style="color:#f92672"&gt;=&lt;/span&gt;&lt;span style="color:#ae81ff"&gt;1&lt;/span&gt; &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;OLLAMA_KV_CACHE_TYPE&lt;span style="color:#f92672"&gt;=&lt;/span&gt;q8_0 &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;OLLAMA_KEEP_ALIVE&lt;span style="color:#f92672"&gt;=&lt;/span&gt;60m &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;OLLAMA_NUM_BATCH&lt;span style="color:#f92672"&gt;=&lt;/span&gt;&lt;span style="color:#ae81ff"&gt;2048&lt;/span&gt; &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;ollama serve
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;What each one does:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;OLLAMA_FLASH_ATTENTION=1&lt;/code&gt;&lt;/strong&gt;: Enables Flash Attention, reducing attention memory from O(n²) to O(n) and speeding up prompt evaluation&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;OLLAMA_KV_CACHE_TYPE=q8_0&lt;/code&gt;&lt;/strong&gt;: Quantizes the KV cache to 8-bit, cutting KV cache memory roughly in half with minimal quality loss. This frees up room for larger context&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;OLLAMA_KEEP_ALIVE=60m&lt;/code&gt;&lt;/strong&gt;: Keeps the model loaded in memory for 60 minutes between requests. Prevents expensive cold-start reloads and enables KV cache reuse&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;OLLAMA_NUM_BATCH=2048&lt;/code&gt;&lt;/strong&gt;: Increases the prompt evaluation batch size from the default 512 to 2048. Processes more tokens per forward pass, speeding up prompt eval by roughly 2-3x. Note: this env var is not in the official Ollama docs but is a known undocumented setting that may change in future versions&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Also set &lt;strong&gt;Context Length&lt;/strong&gt; to &lt;strong&gt;32768&lt;/strong&gt; in the Ollama app settings (menu bar icon &amp;gt; Settings). Ollama&amp;rsquo;s Claude Code docs recommend at least 64K tokens, but 32K is the minimum viable setting given the 64 GB RAM constraint (54 GB model + KV cache). Then quit and reopen Ollama, and warm the model:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;curl http://localhost:11434/api/generate -d &lt;span style="color:#e6db74"&gt;&amp;#39;{
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;&lt;span style="color:#e6db74"&gt; &amp;#34;model&amp;#34;: &amp;#34;qwen3-coder-next:q4_K_M&amp;#34;,
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;&lt;span style="color:#e6db74"&gt; &amp;#34;keep_alive&amp;#34;: &amp;#34;60m&amp;#34;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;&lt;span style="color:#e6db74"&gt;}&amp;#39;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;&lt;strong&gt;Note&lt;/strong&gt;: Do not specify &lt;code&gt;num_ctx&lt;/code&gt; in API calls when the Ollama Mac app GUI already has Context Length configured. Setting it in both places causes the model to fail to load.&lt;/p&gt;
&lt;h3 id="claude-code-client-optimizations"&gt;Claude Code Client Optimizations&lt;/h3&gt;
&lt;p&gt;Launch with the correct autocompaction and effort level:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;ANTHROPIC_AUTH_TOKEN&lt;span style="color:#f92672"&gt;=&lt;/span&gt;ollama &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;ANTHROPIC_BASE_URL&lt;span style="color:#f92672"&gt;=&lt;/span&gt;http://192.168.10.1:11434 &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;ANTHROPIC_API_KEY&lt;span style="color:#f92672"&gt;=&lt;/span&gt;&lt;span style="color:#e6db74"&gt;&amp;#34;&amp;#34;&lt;/span&gt; &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;CLAUDE_AUTOCOMPACT_PCT_OVERRIDE&lt;span style="color:#f92672"&gt;=&lt;/span&gt;&lt;span style="color:#ae81ff"&gt;18&lt;/span&gt; &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;CLAUDE_CODE_EFFORT_LEVEL&lt;span style="color:#f92672"&gt;=&lt;/span&gt;low &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;ANTHROPIC_DEFAULT_HAIKU_MODEL&lt;span style="color:#f92672"&gt;=&lt;/span&gt;qwen3-coder-next:q4_K_M &lt;span style="color:#ae81ff"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;claude --model qwen3-coder-next:q4_K_M
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;The key fix here is &lt;strong&gt;&lt;code&gt;CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=18&lt;/code&gt;&lt;/strong&gt;. Claude Code hardcodes &lt;code&gt;effectiveWindow=180000&lt;/code&gt; for unknown models, and there is no env var to set the actual context window size. By setting autocompaction to trigger at 18% of 180K (~32K tokens), we match the Ollama &lt;code&gt;num_ctx&lt;/code&gt; of 32768. Without this, Claude Code won&amp;rsquo;t compact until 167K tokens, far beyond what Ollama can handle. This works because we enabled q8_0 KV cache quantization, which halves KV cache memory and makes 32K context fit comfortably alongside the 54 GB model in 64 GB RAM.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;CLAUDE_CODE_EFFORT_LEVEL=low&lt;/code&gt;&lt;/strong&gt; reduces the system prompt size and makes the model less verbose, leaving more of the context window for actual conversation and directly reducing prompt eval time.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;ANTHROPIC_DEFAULT_HAIKU_MODEL=qwen3-coder-next:q4_K_M&lt;/code&gt;&lt;/strong&gt; fixes the Haiku 404 errors (root cause #4 above). Claude Code calls &lt;code&gt;claude-haiku-4-5-20251001&lt;/code&gt; for background tasks like token counting. Ollama doesn&amp;rsquo;t have this model, so every call fails with a 404, wasting two HTTP round-trips each time. Pointing it to the same Qwen model eliminates these errors.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Watch out for hallucinated env vars&lt;/strong&gt;: AI assistants (including the one that helped write this post) commonly suggest env vars that don&amp;rsquo;t exist in Claude Code. I initially used &lt;code&gt;ANTHROPIC_MODEL_CONTEXT_LENGTH&lt;/code&gt; and &lt;code&gt;CLAUDE_CODE_MAX_TURNS&lt;/code&gt;, neither of which are real. Always verify against the &lt;a href="https://code.claude.com/docs/en/settings"&gt;official Claude Code settings docs&lt;/a&gt; and &lt;a href="https://code.claude.com/docs/en/model-config"&gt;model configuration docs&lt;/a&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id="inference-tools-for-apple-silicon"&gt;Inference Tools for Apple Silicon&lt;/h2&gt;
&lt;p&gt;If you&amp;rsquo;re running local LLMs on a Mac, there are several inference engines to consider:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Engine&lt;/th&gt;
&lt;th&gt;Backend&lt;/th&gt;
&lt;th&gt;Apple Silicon Optimized&lt;/th&gt;
&lt;th&gt;Best For&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;Ollama&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;llama.cpp + Metal&lt;/td&gt;
&lt;td&gt;Yes&lt;/td&gt;
&lt;td&gt;General use, Claude Code integration&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;llama.cpp&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;Native + Metal&lt;/td&gt;
&lt;td&gt;Yes&lt;/td&gt;
&lt;td&gt;Maximum control, latest features&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;MLX / mlx-lm&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;Apple ML framework&lt;/td&gt;
&lt;td&gt;Best (native Metal)&lt;/td&gt;
&lt;td&gt;Maximum Apple Silicon performance&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;LM Studio&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;llama.cpp&lt;/td&gt;
&lt;td&gt;Yes&lt;/td&gt;
&lt;td&gt;GUI-based experimentation&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;vLLM&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;CUDA-based&lt;/td&gt;
&lt;td&gt;No (CPU fallback)&lt;/td&gt;
&lt;td&gt;NVIDIA GPU servers only&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;strong&gt;Ollama&lt;/strong&gt; is the easiest path for Claude Code since it provides the Anthropic-compatible API that Claude Code expects. &lt;strong&gt;MLX&lt;/strong&gt; is potentially the fastest on Apple Silicon (native Metal optimization with better memory access patterns), but requires model conversion and an API proxy.&lt;/p&gt;
&lt;p&gt;For a detailed technical breakdown of how these engines work internally (KV cache memory math, Flash Attention algorithms, and Apple Silicon memory architecture), read my companion post: &lt;a href="https://vijay.eu/co-authored/llm-inference-internals-apple-silicon/"&gt;LLM Inference Internals: KV Cache, Flash Attention, and Optimizing for Apple Silicon&lt;/a&gt;.&lt;/p&gt;
&lt;h2 id="results-and-conclusion"&gt;Results and Conclusion&lt;/h2&gt;
&lt;p&gt;Here&amp;rsquo;s what changed:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Metric&lt;/th&gt;
&lt;th&gt;Before&lt;/th&gt;
&lt;th&gt;After (Expected)&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Context window used&lt;/td&gt;
&lt;td&gt;Up to 34K+ (unbounded)&lt;/td&gt;
&lt;td&gt;Capped at ~30K (autocompaction at 32K window)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Prompt eval per call&lt;/td&gt;
&lt;td&gt;30–194 seconds&lt;/td&gt;
&lt;td&gt;10–40 seconds&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Batch size&lt;/td&gt;
&lt;td&gt;512 (default)&lt;/td&gt;
&lt;td&gt;2048 (~2–3x faster eval)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Flash Attention&lt;/td&gt;
&lt;td&gt;Off&lt;/td&gt;
&lt;td&gt;On&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;KV cache type&lt;/td&gt;
&lt;td&gt;fp16&lt;/td&gt;
&lt;td&gt;q8_0 (half memory)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;Estimated session time&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;~60 minutes&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;~15–20 minutes&lt;/strong&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The biggest single win was fixing the context window mismatch. That alone prevents the conversation from growing to 2x the model&amp;rsquo;s actual capacity and triggering huge prompt evaluations.&lt;/p&gt;
&lt;p&gt;There&amp;rsquo;s a fundamental reality to accept: a 79.7B model running locally at 20 tok/s prompt eval will always be slower than cloud APIs. Each agentic round-trip inherently takes seconds for prompt evaluation. But the difference between &amp;ldquo;painfully slow&amp;rdquo; and &amp;ldquo;totally usable&amp;rdquo; comes down to configuration. The model was fine. The wiring between Claude Code and Ollama just needed tuning.&lt;/p&gt;
&lt;p&gt;Running local is worth it for privacy and cost. You just need to understand what&amp;rsquo;s happening under the hood.&lt;/p&gt;
&lt;h2 id="follow-me"&gt;Follow me&lt;/h2&gt;
&lt;p&gt;If you are new to my posts, I regularly post about AI, LLMs, AWS, EKS, Kubernetes and Cloud computing related topics. Do follow me in &lt;a href="https://www.linkedin.com/in/vijaykodam/"&gt;LinkedIn&lt;/a&gt; and visit &lt;a href="https://dev.to/vijaykodam"&gt;my dev.to posts&lt;/a&gt;. You can find all my previous blog posts in &lt;a href="https://vijay.eu/posts"&gt;my blog&lt;/a&gt;&lt;/p&gt;</description></item></channel></rss>